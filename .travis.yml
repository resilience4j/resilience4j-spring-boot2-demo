dist: trusty
language: java
sudo: false
matrix:
  include:
    - jdk: oraclejdk8
      env: PUBLISH=true
    #- jdk: openjdk11
before_install:
  - chmod +x gradlew
before_cache:
- rm -f  $HOME/.gradle/caches/modules-2/modules-2.lock
- rm -fr $HOME/.gradle/caches/*/plugin-resolution/